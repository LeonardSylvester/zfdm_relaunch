---
import VideoMd from "@/content/videos/Youtube.md";
interface Props {
  trust_badges?: { icon: string; title: string; description?: string }[];
  showVideo?: boolean;
}

const { trust_badges,showVideo = true } = Astro.props;

// Default trust badges if none provided
const defaultBadges = [
  {
    icon: "🔒",
    title: "DSGVO-konform",
    description: "100% Datenschutz"
  },
  {
    icon: "✓",
    title: "14 Tage testen",
    description: "Kostenlos & unverbindlich"
  },
  {
    icon: "🇩🇪",
    title: "Made in Germany",
    description: "Deutscher Support"
  },
  {
    icon: "❌",
    title: "Kein Abo",
    description: "Einmalige Zahlung"
  }
];

const badges = trust_badges || defaultBadges;
---

<section class="section pt-0">
  <div class="container">
    <div class="row">
      <div class="col-12">
        <div
          class="bg-light dark:bg-darkmode-light rounded-2xl p-8 lg:p-10"
          data-aos="fade-up-sm"
        >
          <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
            {
              badges.map((badge, index) => (
                <div
                  class="text-center"
                  data-aos="fade-up-sm"
                  data-aos-delay={100 + index * 50}
                >
                  <div class="text-5xl mb-3">{badge.icon}</div>
                  <h3 class="h6 mb-2">{badge.title}</h3>
                  {badge.description && (
                    <p class="text-sm text-text-dark dark:text-darkmode-text-dark">
                      {badge.description}
                    </p>
                  )}
                </div>
              ))
            }
          </div>
        </div>
      </div>
    </div>
  </div>
  {showVideo && (
    <div class="mt-8 flex justify-center">
    <VideoMd />
  </div> )};
</section>
